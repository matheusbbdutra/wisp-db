import './App.css';

// Placeholder do skeleton inicial. UI real (editor Monaco, data grid,
// sidebar de schemas) entra nas Fases 1-2 do roadmap (docs/ROADMAP.md).
function App() {
    return (
        <div id="App">
            <div id="result" className="result">Wisp — skeleton inicial</div>
        </div>
    )
}

export default App
